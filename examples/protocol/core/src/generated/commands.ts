// Code-generated by snapscript-cli. Do not edit.
import type { ClientWorld, ReadonlyEntityRef } from "snapscript";
import { internal } from "./protocol";
import type {
  WorldStartGamePayload,
  PeerReadyPayload,
  PlayerMovePayload,
} from "./protocol";

export const commands = {
  World: {
    StartGame(world: ClientWorld, payload: WorldStartGamePayload): void {
      internal.World.commands.StartGame.send(world, payload);
    },
  },
  Peer: {
    Ready(world: ClientWorld, payload: PeerReadyPayload): void {
      internal.Peer.commands.Ready.send(world, payload);
    },
  },
  Player: {
    Move(world: ClientWorld, target: ReadonlyEntityRef, payload: PlayerMovePayload): void {
      internal.Player.commands.Move.send(world, target, payload);
    },
  },
} as const;
