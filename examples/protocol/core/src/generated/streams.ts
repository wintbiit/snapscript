// Code-generated by snapscript-cli. Do not edit.
import type { ClientWorld, ReadonlyEntityRef } from "snapscript";
import { internal } from "./protocol";
import type {
  PlayerMoveStreamPayload,
} from "./protocol";

export const streams = {
  Player: {
    MoveStream(world: ClientWorld, target: ReadonlyEntityRef, payload: PlayerMoveStreamPayload, clientTick: number, dtMs: number): void {
      internal.Player.streams.MoveStream.push(world, target, payload, clientTick, dtMs);
    },
  },
} as const;
