# Transport

`memory.ts` is a test-only transport for local round-trip checks. Production projects should provide
their own platform adapter, such as WebSocket, UDP, WebRTC, or an engine network binding.
