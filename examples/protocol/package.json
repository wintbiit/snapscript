{
  "name": "@snapscript/example-protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "pnpm --dir core check",
    "generate": "pnpm --dir core generate",
    "build": "pnpm --dir core build && pnpm --dir app build",
    "dev": "pnpm --dir core generate && pnpm --dir app dev"
  }
}
